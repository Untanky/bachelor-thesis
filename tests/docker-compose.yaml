version: "3.7"
services:
  database:
    hostname: database
    container_name: database
    image: postgres
    ports:
      - 5432:5432
    volumes:
      - ../database/scripts:/docker-entrypoint-initdb.d
    environment: 
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root

